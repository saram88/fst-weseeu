function sendMail(contactForm) {
    emailjs.send("gmail", "service_vg0sszf", {
      "from_name": contactForm.name.value,
      "from_email": contactForm.projectsummary.value
    })
    then(
        function(response) {
            console.log("SUCCESS", response);
        },
        function(error) {
            console.log("FAILED", error);
        });
}
